import { SimulatorPipePipe } from './simulator-pipe.pipe';

describe('SimulatorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SimulatorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
